NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-K8XWRJ26V2
# New customer PayPal checkout is disabled by default. Set this and the
# backend PAYPAL_ENABLED flag to true, then rebuild, to re-enable it.
NEXT_PUBLIC_PAYPAL_ENABLED=false
